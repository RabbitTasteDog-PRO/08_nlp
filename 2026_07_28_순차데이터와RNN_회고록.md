# Sequence Model 01~03 학습 회고록

> 학습 일자: 2026-07-28  
> 대상: `01_sequential_data_rnn.ipynb`, `02_lstm.ipynb`, `03_gru.ipynb`

이번 학습에서는 순서가 있는 데이터를 다루는 방법을 기본 RNN에서 시작해 LSTM, GRU까지 단계적으로 살펴보았다. 이전의 BoW 기반 접근에서는 단어의 등장 여부나 빈도에 집중했다면, 이번에는 **토큰이 어떤 순서로 등장했는지**와 앞선 정보가 다음 판단에 어떻게 전달되는지를 중심으로 이해했다.

## 1. 순차 데이터와 기본 RNN

첫 노트북에서는 같은 값으로 이루어졌더라도 순서가 반대이면 서로 다른 시퀀스라는 점을 확인했다. 오름차순과 내림차순 시퀀스는 합계가 같지만 위치별 값은 다르다. 이를 통해 단어 집합만 보는 방식으로는 어순이나 시간적 변화를 충분히 표현하기 어렵고, 순서 정보를 유지하는 모델이 필요하다는 것을 다시 느꼈다.

RNN은 현재 입력 `x_t`와 이전 은닉 상태 `h_(t-1)`를 함께 사용해 현재 은닉 상태 `h_t`를 만든다. 특히 `batch_first=True`일 때 입력이 `(B, L, F)` 형식이라는 점을 반복해서 확인했다. 여기서 `B`는 한 번에 처리할 문장 수, `L`은 토큰 수 또는 시퀀스 길이, `F`는 토큰 하나를 표현하는 특성 수이다. 처음에는 shape의 각 축이 무엇을 뜻하는지 헷갈릴 수 있었지만, `(2, 4, 3)` 예제를 통해 “문장 2개, 문장마다 토큰 4개, 토큰당 특성 3개”로 해석하는 기준을 잡을 수 있었다.

또한 RNN이 반환하는 `output`과 `hidden`의 역할을 구분했다. `output`은 모든 타임 스텝의 은닉 상태를 담고, `hidden`은 마지막 시점의 은닉 상태를 담는다. 단층·단방향 RNN에서는 `output[:, -1, :]`와 `hidden[-1]`이 같다는 것을 코드로 검증했다. 문장 분류에서는 이 마지막 은닉 상태를 문장 전체를 요약한 벡터로 보고 `Linear` 계층에 전달해 로짓을 만들 수 있었다.

상승·하강 시퀀스 분류 실습에서는 데이터 생성부터 학습과 검증까지의 흐름을 연결했다. `BCEWithLogitsLoss`를 사용할 때는 모델 출력에 시그모이드를 미리 적용하지 않는 이유, `model.train()`과 `model.eval()`의 사용 목적, 그리고 `torch.no_grad()`가 평가 시 불필요한 기울기 계산을 막는다는 점을 정리할 수 있었다. `clip_grad_norm_()` 역시 RNN에서 발생할 수 있는 기울기 폭주를 완화하는 장치임을 이해했다. 다만 gradient clipping은 기울기 소실 자체를 해결하지는 못한다는 한계도 함께 확인했다.

## 2. LSTM: 기억을 선택적으로 유지하는 구조

기본 RNN은 이전 정보를 은닉 상태 하나에 계속 갱신하기 때문에, 시퀀스가 길어질수록 초반의 중요한 단서가 약해질 수 있다. 두 번째 노트북에서 학습한 LSTM은 이 문제를 완화하기 위해 은닉 상태와 별도로 셀 상태 `C_t`를 둔다. 셀 상태는 장기 기억 경로이고, 은닉 상태는 현재 시점의 출력이자 다음 계산에 전달되는 상태라는 차이를 구분하는 것이 핵심이었다.

LSTM의 망각·입력·출력 게이트는 사람이 미리 규칙을 정하는 것이 아니라 학습을 통해 각 정보의 유지 비율을 찾는다. 특히 다음 상태 갱신식을 직접 따라가며 각 게이트의 역할을 연결할 수 있었다.

```text
C_t = f_t ⊙ C_(t-1) + i_t ⊙ g_t
h_t = o_t ⊙ tanh(C_t)
```

코드에서는 `weight_ih_l0`, `weight_hh_l0`와 편향을 사용해 첫 번째 타임 스텝의 게이트를 직접 계산했고, 수동으로 계산한 `manual_hidden`이 `lstm_output[:, 0, :]`와 일치하는지 확인했다. 단순히 API를 호출하는 데 그치지 않고, PyTorch가 LSTM의 네 계산을 `(i, f, g, o)` 순서로 하나의 큰 가중치 텐서에 저장한다는 구현 방식까지 이해한 점이 의미 있었다.

또한 LSTM은 RNN보다 게이트 계산이 많아 같은 입력·은닉 차원에서 파라미터 수가 늘어난다는 사실을 확인했다. 장기 의존성을 다루기 좋은 구조이지만, 계산량·메모리·과적합 가능성도 함께 고려해야 한다. 따라서 “LSTM이 항상 더 좋다”라고 단정하기보다는 동일한 데이터 분할과 평가 기준에서 성능을 비교해야 한다는 기준을 세웠다.

마지막으로 다층 LSTM과 양방향 LSTM의 shape도 살펴보았다. `num_layers=2`에서는 층별 최종 상태가 쌓이고, 양방향 설정에서는 순방향과 역방향 결과가 특성 축에 결합되어 출력 차원이 두 배가 된다. 양방향 구조는 전체 문장을 이미 확보한 감성 분류에는 적합할 수 있지만, 미래 토큰을 볼 수 없는 실시간 예측이나 생성 작업에는 그대로 적용하기 어렵다는 점도 기억할 필요가 있다.

## 3. GRU: 더 단순한 게이트 기반 순환 모델

세 번째 노트북에서는 GRU를 통해 LSTM과 유사한 목적을 더 단순한 구조로 달성하는 방식을 학습했다. GRU는 별도의 셀 상태 없이 은닉 상태 하나만 사용하며, 리셋 게이트와 업데이트 게이트로 이전 정보와 새 후보 정보를 조절한다.

```text
r_t = sigmoid(...)
z_t = sigmoid(...)
n_t = tanh(...)
h_t = z_t ⊙ h_(t-1) + (1 - z_t) ⊙ n_t
```

여기서 리셋 게이트는 후보 상태를 만들 때 이전 기억을 얼마나 참고할지 조절하고, 업데이트 게이트는 이전 상태와 새 후보 상태를 어느 정도 섞을지 결정한다. 특히 업데이트 게이트의 해석은 구현마다 식 표기가 다를 수 있으므로, 이번에는 PyTorch의 식을 기준으로 `z_t`가 1에 가까울수록 이전 상태를 더 유지한다고 이해했다.

GRU도 첫 타임 스텝을 직접 계산해 `nn.GRU`의 출력과 일치하는지 검증했다. 이 과정을 통해 RNN·LSTM·GRU의 차이가 단순히 반환값 개수가 아니라, “이전 정보를 어떤 경로로 보존하고 갱신하는가”에 있다는 점이 더 분명해졌다. 파라미터 수 비교에서도 같은 조건에서 RNN 50개, GRU 150개, LSTM 200개로 확인되어, GRU가 LSTM보다 가벼운 구조라는 설명을 수치로 확인할 수 있었다.

실제 텍스트 분류 흐름까지 연결한 점도 중요했다. 토큰 ID `(B, L)`는 `Embedding`을 거쳐 `(B, L, E)`가 되고, GRU의 마지막 은닉 상태 `(B, H)`를 `Linear` 계층에 전달해 문장별 로짓 `(B,)`을 만든다. 길이가 다른 문장을 배치로 처리할 때는 PAD를 추가하고 `pack_padded_sequence()`에 실제 길이를 전달해 PAD가 상태 갱신에 영향을 주지 않게 했다. 이는 이후 NSMC처럼 실제 리뷰 데이터를 다룰 때 반드시 적용해야 할 실무적인 처리라고 생각한다.

## 4. RNN·LSTM·GRU 비교를 통해 정리한 기준

| 모델 | 상태·게이트 구성 | 장점 | 고려할 점 |
| --- | --- | --- | --- |
| RNN | 은닉 상태 1개, 게이트 없음 | 구조가 단순하고 기준선 모델로 적합 | 긴 문맥에서 기울기 소실과 기억 손실이 발생하기 쉬움 |
| LSTM | 은닉 상태·셀 상태, 3개 게이트 | 장기 정보를 선택적으로 보존하기 쉬움 | 파라미터와 계산량이 가장 큼 |
| GRU | 은닉 상태 1개, 2개 게이트 | LSTM보다 단순하고 비교적 적은 파라미터 | LSTM보다 항상 좋은 것은 아니므로 검증 필요 |

결국 모델 선택은 구조 이름만으로 결정할 수 없다. 실제 데이터에서 validation loss, accuracy, F1, 학습 시간과 메모리 사용량을 함께 비교해야 한다. 특히 감성 분류에서는 단순 정확도뿐 아니라 클래스 불균형 여부와 오분류 사례도 확인해야 한다.

## 5. 핵심 실습 및 예제 코드

아래 코드는 세 노트북에서 다룬 핵심 실습만 다시 실행할 수 있도록 정리한 것이다. 모든 예제는 `batch_first=True`를 사용하므로 입력 축 순서는 `(배치, 시퀀스 길이, 특성 수)`이다.

### 5-1. 기본 RNN의 입력·출력·마지막 은닉 상태 확인

```python
import torch
import torch.nn as nn

torch.manual_seed(42)

B, L, F, H = 2, 4, 3, 5
inputs = torch.arange(B * L * F, dtype=torch.float32)
inputs = inputs.reshape(B, L, F) / 10

rnn = nn.RNN(input_size=F, hidden_size=H, batch_first=True)
output, hidden = rnn(inputs)

print("입력 shape:", inputs.shape)    # (2, 4, 3)
print("output shape:", output.shape)  # (2, 4, 5)
print("hidden shape:", hidden.shape)  # (1, 2, 5)

# 단층·단방향 RNN에서는 두 값이 같은 마지막 은닉 상태를 가리킨다.
print(torch.allclose(output[:, -1, :], hidden[-1]))  # True
```

이 실습에서 확인할 점은 `output`에는 모든 토큰 위치의 상태가 있고, `hidden[-1]`에는 마지막 토큰까지 읽은 문장별 상태가 있다는 것이다.

### 5-2. 마지막 은닉 상태를 사용하는 RNN 이진 분류기

```python
class SequenceRNNClassifier(nn.Module):
    def __init__(self, input_size, hidden_size):
        super().__init__()
        self.rnn = nn.RNN(
            input_size=input_size,
            hidden_size=hidden_size,
            batch_first=True,
        )
        self.classifier = nn.Linear(hidden_size, 1)

    def forward(self, inputs):
        _, hidden = self.rnn(inputs)
        last_hidden = hidden[-1]                 # (B, H)
        return self.classifier(last_hidden).squeeze(1)  # (B,)


model = SequenceRNNClassifier(input_size=3, hidden_size=6)
logits = model(inputs)

print("로짓 shape:", logits.shape)  # (2,)
```

이진 분류 학습에서는 로짓을 그대로 `BCEWithLogitsLoss`에 전달한다. 손실 함수 내부에 시그모이드 계산이 포함되어 있으므로, 학습 단계에서 모델 출력에 `sigmoid()`를 먼저 적용하지 않는다.

```python
criterion = nn.BCEWithLogitsLoss()
optimizer = torch.optim.Adam(model.parameters(), lr=0.01)

targets = torch.tensor([1.0, 0.0])
optimizer.zero_grad()
logits = model(inputs)
loss = criterion(logits, targets)
loss.backward()

# 긴 시퀀스에서 큰 기울기를 제한한다.
torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)
optimizer.step()

probabilities = torch.sigmoid(logits)
predictions = (probabilities >= 0.5).long()
```

### 5-3. LSTM 게이트와 셀 상태를 직접 계산하기

```python
torch.manual_seed(42)

B, L, F, H = 2, 4, 3, 5
inputs = torch.arange(B * L * F, dtype=torch.float32)
inputs = inputs.reshape(B, L, F) / 10

lstm = nn.LSTM(input_size=F, hidden_size=H, batch_first=True)
lstm_output, (lstm_hidden, lstm_cell) = lstm(inputs)

# 첫 토큰 위치의 계산을 PyTorch 가중치로 직접 재현한다.
x1 = inputs[:, 0, :]
h0 = torch.zeros(B, H)
c0 = torch.zeros(B, H)

gate_linear = (
    x1 @ lstm.weight_ih_l0.T + lstm.bias_ih_l0
    + h0 @ lstm.weight_hh_l0.T + lstm.bias_hh_l0
)
i_linear, f_linear, g_linear, o_linear = gate_linear.chunk(4, dim=1)

i = torch.sigmoid(i_linear)  # 입력 게이트
f = torch.sigmoid(f_linear)  # 망각 게이트
g = torch.tanh(g_linear)     # 새 기억 후보
o = torch.sigmoid(o_linear)  # 출력 게이트

c1 = f * c0 + i * g
h1 = o * torch.tanh(c1)

print(torch.allclose(h1, lstm_output[:, 0, :], atol=1e-6))  # True
print(lstm_hidden.shape, lstm_cell.shape)  # 둘 다 (1, 2, 5)
```

핵심은 LSTM이 `output`, `hidden` 외에 장기 기억인 `cell`도 반환한다는 점이다. `hidden`과 `cell`은 shape가 같아도 서로 다른 값을 가진다.

### 5-4. GRU로 PAD를 제외한 문장 분류하기

```python
from torch.nn.utils.rnn import pack_padded_sequence


class GRUSentenceClassifier(nn.Module):
    def __init__(self, vocabulary_size, embedding_dim, hidden_size):
        super().__init__()
        self.embedding = nn.Embedding(
            vocabulary_size,
            embedding_dim,
            padding_idx=0,
        )
        self.gru = nn.GRU(embedding_dim, hidden_size, batch_first=True)
        self.classifier = nn.Linear(hidden_size, 1)

    def forward(self, token_ids, lengths):
        embedded = self.embedding(token_ids)  # (B, L, E)
        packed = pack_padded_sequence(
            embedded,
            lengths.cpu(),
            batch_first=True,
            enforce_sorted=False,
        )
        _, hidden = self.gru(packed)
        sentence_vector = hidden[-1]         # (B, H)
        return self.classifier(sentence_vector).squeeze(1)


# PAD는 0, 실제 단어 ID는 1부터 시작한다.
token_ids = torch.tensor([
    [1, 2, 3],  # 실제 길이 3
    [2, 4, 0],  # 실제 길이 2, 마지막 0은 PAD
])
lengths = torch.tensor([3, 2])

gru_model = GRUSentenceClassifier(
    vocabulary_size=5,
    embedding_dim=4,
    hidden_size=5,
)
logits = gru_model(token_ids, lengths)
print(logits.shape)  # (2,)
```

`pack_padded_sequence()`을 사용하면 짧은 문장의 PAD 위치가 GRU의 상태 갱신에 반영되지 않는다. 실제 NSMC 데이터처럼 리뷰 길이가 제각각인 경우에 특히 중요하다.

### 5-5. RNN·LSTM·GRU의 파라미터 수 비교

```python
def count_trainable_parameters(model):
    return sum(p.numel() for p in model.parameters() if p.requires_grad)


input_size, hidden_size = 3, 5
models = {
    "RNN": nn.RNN(input_size, hidden_size, batch_first=True),
    "GRU": nn.GRU(input_size, hidden_size, batch_first=True),
    "LSTM": nn.LSTM(input_size, hidden_size, batch_first=True),
}

for name, model in models.items():
    print(name, count_trainable_parameters(model))

# 이 설정에서는 RNN=50, GRU=150, LSTM=200
```

같은 입력·은닉 차원에서는 게이트 수가 많을수록 파라미터 수도 증가한다. 다만 파라미터 수만으로 성능을 판단하지 말고, 실제 validation 지표와 학습 시간을 함께 비교해야 한다.

## 6. 추가 실습 과제

1. **순서 민감성 확인:** 오름차순·내림차순 시퀀스에 노이즈를 추가해 RNN 분류기를 학습하고, 검증 데이터에서 상승 확률을 출력해 본다.
2. **LSTM 구조 변경:** `num_layers=2` 또는 `bidirectional=True`를 적용한 뒤 `output`, `hidden`, `cell`의 shape가 어떻게 달라지는지 기록한다.
3. **모델 공정 비교:** 동일한 임베딩 크기, hidden size, epoch, 데이터 분할로 RNN·LSTM·GRU를 각각 학습하고 validation loss, accuracy, F1, 학습 시간을 표로 비교한다.
4. **PAD 처리 비교:** `pack_padded_sequence()`를 쓴 GRU와 쓰지 않은 GRU를 비교해, 짧은 문장의 마지막 상태가 PAD에 의해 달라질 수 있음을 확인한다.
5. **오분류 분석:** NSMC 리뷰에서 틀린 예측을 10개 추출해 부정 표현, 반전 표현, OOV 단어, 긴 문장 중 무엇이 원인인지 분류한다.

## 7. 이번 학습에서 얻은 점과 다음 목표

이번 학습의 가장 큰 수확은 순환 모델을 “문장을 한 번에 처리하는 블랙박스”가 아니라, 각 토큰 위치에서 상태를 갱신하며 문맥을 전달하는 구조로 이해하게 된 점이다. 또한 shape를 해석하고, 마지막 은닉 상태를 분류기로 연결하며, 수동 계산으로 프레임워크 내부 구현을 검증하는 흐름을 경험했다.

다음 단계에서는 NSMC 리뷰 데이터에 임베딩과 RNN/LSTM/GRU를 실제로 적용해 보고 싶다. 이때는 다음을 중점적으로 확인할 것이다.

- 토크나이징, 단어 사전 구축, PAD·UNK 처리의 기준을 일관되게 정하기
- 가변 길이 문장에 `pack_padded_sequence()` 또는 마스킹을 올바르게 적용하기
- RNN·LSTM·GRU를 같은 데이터 분할과 하이퍼파라미터 조건에서 비교하기
- validation loss·accuracy·F1과 오분류 리뷰를 함께 분석하기
- 양방향 모델, dropout, hidden size 등의 변화가 성능과 과적합에 미치는 영향 확인하기

이번 01~03 노트북은 이후 실제 한국어 감성 분류 모델을 학습하기 전에 필요한 개념적·구현적 기반을 만든 과정이었다. 다음 실습에서는 이 기반을 바탕으로 “모델의 내부 구조 이해”를 “실제 텍스트 성능 검증”까지 확장하는 것을 목표로 한다.
