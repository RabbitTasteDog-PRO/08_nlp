# 2026-07-27 텍스트 벡터화 학습 회고

어제는 텍스트 벡터화를 학습했다. 처음에는 문장을 숫자로 바꾸는 작업 정도로 생각했는데, 내용을 따라가다 보니 결국은 컴퓨터가 텍스트를 어떤 방식으로 바라보게 할지 정하는 과정에 더 가깝다는 생각이 들었다.

게임을 만들 때는 문자열을 화면에 출력하거나 조건문에 활용하는 경우가 많았다. 하지만 자연어 처리에서는 문장을 그대로 계산할 수 없기 때문에, 단어·문장·문서 같은 대상을 숫자 벡터로 바꿔야 한다. 같은 문장이라도 어떤 정보를 남기고 어떤 정보를 버리느냐에 따라 결과가 달라진다는 점이 이번 학습의 핵심이었다.

## BoW와 TF-IDF

가장 먼저 BoW와 TF-IDF를 다뤘다. BoW는 문서에 어떤 단어가 몇 번 등장했는지를 세는 방식이라 비교적 직관적으로 이해됐다. 문서를 행으로, 단어를 열로 두면 문서-단어 행렬(DTM)이 만들어지고, 단어 빈도를 표처럼 확인할 수 있었다.

다만 BoW는 단어 순서를 보존하지 않는다. `I like this movie`와 `I do not like this movie`는 의미가 다르지만, 단어 빈도만으로는 부정 표현의 영향을 충분히 반영하기 어렵다. 단어를 숫자로 바꾸는 일이 단순해 보여도, 어떤 정보를 놓치게 되는지 함께 생각해야 한다는 점을 알게 됐다.

TF-IDF는 여러 문서에 흔하게 등장하는 단어의 비중은 낮추고, 특정 문서를 구분하는 데 도움이 되는 단어의 비중은 높이는 방식이다. 처음에는 IDF 공식이 낯설었지만, 모든 문서에 자주 나오는 단어는 문서를 구분하는 정보가 적다는 관점으로 보니 필요한 이유가 이해됐다. 라이브러리 결과만 확인하는 것보다 TF, DF, IDF와 L2 정규화 과정을 직접 계산해 보는 것이 도움이 됐다.

아래는 같은 문서를 BoW와 TF-IDF로 바꾸고, 결과 shape과 어휘를 확인한 예제다.

```python
from sklearn.feature_extraction.text import CountVectorizer, TfidfVectorizer
from sklearn.metrics.pairwise import cosine_similarity

documents = [
    "I love my dog",
    "I love my cat",
    "my dog loves a walk",
]

count_vectorizer = CountVectorizer()
bow_matrix = count_vectorizer.fit_transform(documents)

tfidf_vectorizer = TfidfVectorizer()
tfidf_matrix = tfidf_vectorizer.fit_transform(documents)

print("BoW 어휘:", count_vectorizer.get_feature_names_out())
print("BoW shape:", bow_matrix.shape)
print("TF-IDF shape:", tfidf_matrix.shape)
print("TF-IDF 유사도:\n", cosine_similarity(tfidf_matrix))
```

## Word2Vec과 임베딩

원-핫 인코딩은 단어를 명확하게 구분할 수 있지만, 단어 사이의 의미 관계를 표현하지 못한다. `king`과 `queen`은 서로 다른 위치에 1이 있을 뿐, 두 단어가 비슷한 맥락에서 쓰인다는 정보는 담기지 않는다.

Word2Vec은 주변 단어를 예측하는 과정에서 단어를 밀집 벡터로 학습한다. 비슷한 문맥에 등장하는 단어가 벡터 공간에서 가까워질 수 있다는 개념이 인상적이었다. 다만 작은 코퍼스로 학습한 유사 단어 결과는 생각보다 불안정할 수 있었다. 벡터 유사도가 높다고 해서 사전적 의미가 같거나 사실 관계가 맞는 것은 아니라는 점도 함께 기억해야겠다.

한국어 Word2Vec에서는 토큰화가 특히 중요했다. 한국어는 조사와 어미가 붙기 때문에 공백만으로 단어를 나누면 활용형이 너무 많이 생길 수 있다. 형태소 분석과 기본형 정규화를 거친 뒤 문장별 토큰 목록을 모델에 전달하는 이유를 이해했다. 앞에서 배운 불용어 제거와 표제어 추출도 벡터화 이전 단계와 자연스럽게 이어졌다.

작은 문장별 토큰 코퍼스로 Skip-gram Word2Vec을 학습하고, 단어 벡터와 유사도를 확인한 코드는 다음과 같다.

```python
from gensim.models import Word2Vec

sentences = [
    ["king", "is", "a", "royal", "man"],
    ["queen", "is", "a", "royal", "woman"],
    ["prince", "is", "a", "young", "royal"],
    ["apple", "is", "a", "sweet", "fruit"],
] * 200

model = Word2Vec(
    sentences=sentences,
    vector_size=20,
    window=2,
    min_count=1,
    sg=1,
    epochs=100,
    workers=1,
    seed=42,
)

print("임베딩 행렬 shape:", model.wv.vectors.shape)
print("king 벡터 shape:", model.wv["king"].shape)
print("king-queen:", model.wv.similarity("king", "queen"))
print("king-apple:", model.wv.similarity("king", "apple"))
```

## 가장 어려웠던 PyTorch `nn.Embedding`

이번 내용 중 가장 어려웠던 부분은 PyTorch의 `nn.Embedding`이었다. 처음에는 단어 ID가 왜 임베딩 행렬의 행 번호가 되는지, 단어 ID 자체에 의미가 있는 것은 아닌지 헷갈렸다. 하나씩 출력해 보니 ID는 단어의 크기나 중요도를 뜻하는 숫자가 아니라, 해당 단어 벡터를 찾아가기 위한 주소에 가깝다는 것을 알게 됐다.

문장 길이를 맞추기 위한 `<PAD>` 토큰도 중요했다. 짧은 문장 뒤에 PAD를 붙여 배치 텐서를 만들 수는 있지만, 평균 임베딩을 계산할 때 PAD까지 포함하면 실제 단어 벡터의 값이 작아질 수 있다. 그래서 마스크를 만들고 실제 토큰 위치만 평균에 포함해야 했다. `입력 ID → 임베딩 조회 → PAD 제외 평균 → 분류 logits` 순서와 각 단계의 shape을 계속 확인하니 조금씩 흐름이 보였다.

특히 `CrossEntropyLoss`에는 softmax를 적용하기 전의 logits를 넣어야 한다는 점, `padding_idx`는 PAD 행을 학습에서 제외하는 데 도움을 준다는 점을 정리했다. 아직 익숙하지는 않지만, 앞으로 텐서 코드를 볼 때는 값보다 먼저 shape과 dtype을 확인하는 습관을 들여야겠다.

아래 코드는 정수 토큰 ID를 임베딩으로 조회한 뒤, PAD를 제외해 문장 평균 벡터를 만드는 최소 예제다.

```python
import torch
import torch.nn as nn

word_to_id = {"<PAD>": 0, "good": 1, "movie": 2, "not": 3, "bad": 4}
input_ids = torch.tensor([
    [1, 2, 0, 0],
    [3, 4, 2, 0],
], dtype=torch.long)

embedding = nn.Embedding(
    num_embeddings=len(word_to_id),
    embedding_dim=4,
    padding_idx=word_to_id["<PAD>"],
)

embedded = embedding(input_ids)                 # (문장 수, 최대 길이, 임베딩 차원)
mask = input_ids.ne(word_to_id["<PAD>"]).unsqueeze(-1)
masked_sum = (embedded * mask).sum(dim=1)
token_count = mask.sum(dim=1).clamp(min=1)
sentence_vectors = masked_sum / token_count

print("임베딩 shape:", embedded.shape)
print("문장 벡터 shape:", sentence_vectors.shape)
print("PAD 벡터:", embedding.weight[0])
```

## FastText와 OOV

FastText는 Word2Vec의 OOV 문제를 문자 n-gram으로 완화한다. 예를 들어 학습에 `apple`만 있어도 `apples`는 일부 문자 조각을 공유하므로 벡터를 만들 수 있다. 처음 보는 단어를 완전히 포기하지 않고 처리할 수 있다는 점이 장점으로 느껴졌다.

하지만 FastText가 OOV 벡터를 반환한다고 해서 새 단어의 의미를 정확히 이해했다는 뜻은 아니다. 철자가 비슷한 단어가 의미까지 비슷하지는 않고, 임의 문자열에도 벡터가 만들어질 수 있다. 어떤 모델이든 결과를 바로 정답으로 받아들이기보다 데이터와 실제 과업 성능으로 검증해야 한다는 점을 다시 확인했다.

아래 예제에서는 Word2Vec과 FastText가 학습하지 않은 `apples`를 어떻게 다르게 처리하는지 확인할 수 있다.

```python
from gensim.models import FastText, Word2Vec

training_sentences = [
    ["apple", "is", "a", "sweet", "fruit"],
    ["banana", "is", "a", "yellow", "fruit"],
    ["teach", "a", "student", "with", "a", "book"],
] * 200

settings = dict(
    sentences=training_sentences,
    vector_size=20,
    window=2,
    min_count=1,
    sg=1,
    epochs=100,
    workers=1,
    seed=42,
)

word2vec_model = Word2Vec(**settings)
fasttext_model = FastText(**settings, min_n=3, max_n=6, bucket=10_000)

print("Word2Vec 등록 여부:", "apples" in word2vec_model.wv)
print("FastText 등록 여부:", "apples" in fasttext_model.wv.key_to_index)
print("FastText OOV 벡터 shape:", fasttext_model.wv.get_vector("apples").shape)
print("apple-apples 유사도:", fasttext_model.wv.similarity("apple", "apples"))
```

## 다음 학습 목표

이번 학습을 통해 BoW·TF-IDF, Word2Vec, FastText, 문맥 임베딩은 서로 경쟁하는 하나의 정답이 아니라 목적에 따라 선택하는 방법이라는 것을 배웠다. 해석 가능한 빠른 기준선이 필요하면 TF-IDF를 먼저 사용할 수 있고, OOV가 많으면 FastText를 고려할 수 있으며, 문맥에 따라 단어 의미가 달라져야 한다면 BERT 계열 표현을 검토할 수 있다.

다음에는 TF-IDF 기반 분류와 평균 임베딩 기반 분류를 같은 데이터로 직접 비교해 보고 싶다. 또한 `nn.Embedding`은 단어 사전 만들기, 패딩, 마스킹, 학습과 평가를 순서대로 다시 복습해서, 혼자서도 간단한 텍스트 분류 모델을 만들 수 있는 수준까지 익히는 것을 목표로 하겠다.
