# 2026-07-29 텍스트 분류 모델 학습 회고

> 학습 대상: `05_text_classification`  
> 노트북: Naive Bayes, LSTM 뉴스 분류, 멀티레이블 분류, TextCNN

이번 학습에서는 텍스트를 분류하는 방법을 하나의 모델로만 보지 않고, 데이터의 정답 형식과 문장의 특징에 따라 서로 다른 접근을 선택하는 과정을 살펴보았다. 이전 텍스트 벡터화 학습에서는 문장을 수치 벡터로 바꾸는 데 집중했다면, 이번에는 그 벡터를 사용해 실제로 클래스를 예측하고, 기준선·평가 지표·오분류까지 연결해 보았다.

가장 중요하게 느낀 점은 “점수가 높은 모델”을 바로 선택하기보다, **문제의 레이블 구조와 데이터 크기, 필요한 문맥 정보, 평가 기준**을 먼저 확인해야 한다는 것이다. 단어 빈도 기반 Naive Bayes, 순서를 읽는 LSTM, 여러 정답을 동시에 예측하는 One-vs-Rest, 지역 단어 패턴을 찾는 TextCNN은 모두 텍스트 분류라는 같은 목적을 가지지만 입력과 출력의 해석이 달랐다.

## 1. Naive Bayes: 빠르고 해석 가능한 기준선

첫 번째 노트북에서는 CountVectorizer로 문장을 문서-단어 행렬로 바꾸고, Multinomial Naive Bayes가 클래스별 단어 분포를 바탕으로 새 문장의 확률을 계산하는 과정을 학습했다. `재미`, `추천` 같은 단어가 긍정 문장에 자주 등장하고 `지루`, `비추천`이 부정 문장에 자주 등장하면, 모델은 각 클래스에서 단어가 등장할 조건부 확률을 학습한다.

Naive Bayes의 독립 가정은 실제 자연어와 완전히 맞지는 않는다. 예를 들어 `전혀 재미없다`에서 `전혀`와 `재미없다`는 서로 관련이 있지만 모델은 단어들을 독립적으로 본다. 그럼에도 작은 데이터에서 빠르게 학습할 수 있고, 클래스별 주요 단어를 확인하기 쉽기 때문에 좋은 기준선이 될 수 있다는 점을 알게 됐다.

실제 뉴스 분류에서는 TF-IDF와 1-gram·2-gram을 사용했다. TF-IDF는 여러 문서에 공통으로 나오는 단어의 비중을 낮추고 특정 주제를 구분하는 단어의 비중을 높여 준다. 또한 테스트 데이터에는 반드시 `transform()`만 적용해야 한다는 점을 다시 확인했다. 테스트 데이터까지 `fit()`하면 테스트 문서의 어휘와 문서 빈도 정보가 학습 과정에 들어가므로 데이터 누수가 발생한다.

```python
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.naive_bayes import MultinomialNB

tfidf = TfidfVectorizer(
    stop_words="english",
    ngram_range=(1, 2),
    max_features=12_000,
    min_df=2,
    sublinear_tf=True,
)

X_train = tfidf.fit_transform(train_texts)
X_test = tfidf.transform(test_texts)  # 테스트에는 transform만 사용

model = MultinomialNB(alpha=1.0)
model.fit(X_train, y_train)
predictions = model.predict(X_test)
```

또한 모델 성능을 볼 때는 항상 최빈 클래스만 예측하는 `DummyClassifier` 같은 기준선과 비교해야 한다. 특히 클래스 비율이 불균형한 데이터에서는 accuracy 하나만으로 모델의 가치를 판단하기 어렵다. macro F1은 클래스별 F1을 같은 비중으로 평균하므로, 소수 클래스 성능이 무시되지 않는다는 점도 기억해 두어야겠다.

## 2. LSTM: 토큰 순서를 문맥으로 연결하는 분류기

두 번째 노트북에서는 같은 뉴스 주제 분류를 LSTM으로 수행했다. Naive Bayes와 TF-IDF는 단어와 n-gram의 등장 정보를 중심으로 보지만, LSTM은 토큰을 순서대로 읽으면서 이전 위치의 정보를 은닉 상태에 전달한다. 따라서 단어 순서와 앞뒤 문맥이 예측에 중요할 때 더 적합한 표현을 학습할 수 있다.

실습 흐름은 다음과 같이 정리할 수 있다.

```text
원문 → 토큰화 → train 전용 어휘 사전 → 토큰 ID와 PAD → Embedding
     → LSTM → 마지막 은닉 상태 → Linear → 클래스별 로짓
```

여기서 특히 중요했던 것은 PAD와 OOV 처리였다. PAD ID는 0으로 두고 문장 길이를 맞추며, 학습 어휘에 없는 단어는 OOV ID로 바꾼다. 단어 ID 숫자 자체에는 의미나 순서가 없고, 임베딩 행렬에서 해당 단어 벡터를 찾기 위한 인덱스일 뿐이라는 점을 이전 학습과 연결해 이해했다.

LSTM 분류기의 입력과 출력 shape도 반복해서 확인했다. 토큰 ID 입력은 `(B, L)`, Embedding을 통과하면 `(B, L, E)`, 마지막 은닉 상태는 `(B, H)`, 분류 로짓은 `(B, C)`가 된다. 여기서 `B`는 배치 크기, `L`은 최대 토큰 길이, `E`는 임베딩 차원, `H`는 은닉 상태 차원, `C`는 뉴스 주제 클래스 수이다.

```python
class NewsLSTMClassifier(nn.Module):
    def __init__(self, vocab_size, embedding_dim, hidden_dim, num_classes):
        super().__init__()
        self.embedding = nn.Embedding(vocab_size, embedding_dim, padding_idx=0)
        self.lstm = nn.LSTM(embedding_dim, hidden_dim, batch_first=True)
        self.classifier = nn.Linear(hidden_dim, num_classes)

    def forward(self, token_ids):
        embedded = self.embedding(token_ids)       # (B, L, E)
        _, (hidden, _) = self.lstm(embedded)
        return self.classifier(hidden[-1])         # (B, C)
```

다중 클래스 분류에서는 `CrossEntropyLoss`에 softmax 이전의 로짓과 정수형 정답 ID를 전달한다. 손실 함수 내부에 softmax 계산이 포함되어 있으므로, 학습 중 모델 출력에 softmax를 먼저 적용하지 않는 것이 중요하다. 평가에서는 `argmax(dim=1)`로 가장 큰 로짓의 클래스 ID를 선택한다.

이번 실습을 통해 LSTM이 무조건 Naive Bayes보다 좋은 것이 아니라는 점도 생각하게 됐다. LSTM은 임베딩과 순환 계층을 함께 학습해야 하므로 데이터·학습 시간·하이퍼파라미터에 더 민감하다. 따라서 같은 train/validation/test 분할에서 기준선과 비교하고, validation loss가 가장 낮았던 가중치를 저장해 test에 한 번만 평가하는 과정이 필요하다.

## 3. 멀티레이블 분류: 정답 형식과 임계값의 차이

세 번째 노트북에서는 한 문장에 정답이 하나만 있는 단일 레이블 분류와, 여러 정답이 동시에 가능한 멀티레이블 분류의 차이를 학습했다. 영화 설명 문장에 `판타지`, `모험`, `로맨스`가 함께 붙을 수 있으므로, 정답은 하나의 클래스 ID가 아니라 여러 0과 1로 이루어진 multi-hot 벡터가 된다.

예를 들어 레이블 순서가 `[판타지, 모험, 로맨스]`일 때 `판타지`와 `모험`을 함께 가진 문장은 `[1, 1, 0]`으로 표현할 수 있다. 이때 `MultiLabelBinarizer`가 레이블 열 순서를 일관되게 유지하게 해 준다.

멀티레이블 실습에서는 TF-IDF와 `OneVsRestClassifier(LogisticRegression(...))`를 사용했다. 각 레이블마다 “이 레이블이 맞는가?”를 독립적으로 판단하므로, 확률이 가장 큰 하나만 뽑는 `argmax`가 아니라 레이블별 임계값을 적용해야 한다. 기본 임계값인 0.5가 언제나 최선은 아니며, validation 데이터에서 여러 임계값을 비교한 뒤 선택해야 한다는 점이 인상적이었다.

평가 지표도 문제 구조에 맞춰 선택해야 한다. micro F1은 모든 레이블 결정을 합쳐 계산하므로 자주 등장하는 레이블의 영향을 크게 받는다. macro F1은 레이블별 F1을 평균하므로 드문 레이블의 성능도 확인할 수 있다. Hamming loss는 문장-레이블 조합 중 틀린 비율을 보므로, 부분적으로 맞춘 예측을 해석할 때 도움이 된다.

## 4. TextCNN: 지역 단어 패턴을 찾는 방법

마지막 노트북에서는 Conv1d 기반 TextCNN으로 감성 문장을 분류했다. CNN은 이미지에서 지역적인 모양을 찾는 데 자주 쓰이지만, 텍스트에서는 연속된 단어 묶음에서 감성이나 주제를 나타내는 패턴을 찾는 데 활용할 수 있다. 예를 들어 `정말 재미있는`, `전혀 좋지 않은` 같은 연속 표현을 서로 다른 크기의 커널로 탐색할 수 있다.

TextCNN의 텐서 흐름은 다음과 같다.

```text
토큰 ID (B, L) → Embedding (B, L, E) → transpose (B, E, L)
→ Conv1d(여러 커널 크기) → ReLU → Global Max Pooling
→ 특징 연결 → Dropout → Linear → 로짓 (B, 2)
```

처음에는 Conv1d가 `(B, L, E)`를 바로 받지 않는다는 점이 헷갈렸다. PyTorch의 Conv1d는 채널 축을 두 번째에 두므로, 임베딩 결과를 `(B, E, L)`로 바꾸기 위해 `transpose(1, 2)`가 필요하다. 커널 크기 2·3·4를 병렬로 적용하면 각각 다른 길이의 지역 패턴을 찾을 수 있고, Global Max Pooling은 위치마다 가장 강하게 반응한 특징 하나를 남긴다.

TextCNN의 장점은 순환 계산 없이 병렬적으로 지역 패턴을 추출할 수 있다는 점이다. 반면 매우 긴 거리의 의존 관계를 직접 기억하는 데는 한계가 있을 수 있다. 또한 OOV 단어가 많으면 서로 다른 문장도 같은 OOV ID 배열로 바뀌어 같은 예측을 낼 수 있다는 실습 결과를 통해, 모델 구조뿐 아니라 어휘 사전과 전처리 품질도 중요하다는 것을 다시 느꼈다.

## 5. 네 접근을 비교하며 세운 모델 선택 기준

| 접근 | 적합한 상황 | 강점 | 확인할 점 |
| --- | --- | --- | --- |
| TF-IDF + Naive Bayes | 빠른 기준선, 적은 데이터, 해석이 중요한 경우 | 학습이 빠르고 주요 단어를 확인하기 쉬움 | 단어 순서와 긴 문맥을 충분히 반영하기 어려움 |
| Embedding + LSTM | 토큰 순서와 문맥이 중요한 분류 | 순차 정보를 누적해 문장 벡터를 만듦 | PAD, OOV, validation, 학습 시간 관리 필요 |
| One-vs-Rest 멀티레이블 | 문장 하나에 여러 정답이 가능한 경우 | 레이블별 확률과 임계값을 독립적으로 다룰 수 있음 | multi-hot 형식과 threshold·F1 해석 필요 |
| TextCNN | 짧은 지역 단어 패턴이 중요한 경우 | 여러 n-gram 패턴을 병렬로 탐색 가능 | 입력 축 변환과 OOV 한계 확인 필요 |

결국 모델을 선택하기 전에 먼저 확인할 질문은 다음과 같다.

1. 문장마다 정답이 정확히 하나인가, 여러 개인가?
2. 단어의 순서와 긴 문맥이 예측에 중요한가?
3. 빠르고 해석 가능한 기준선이 먼저 필요한가?
4. accuracy 외에 macro F1, micro F1, Hamming loss 중 어떤 지표가 문제 목적에 맞는가?
5. 오분류 문장은 어떤 표현, OOV, 데이터 부족 때문에 틀렸는가?

## 6. 다음 학습 목표

이번 학습을 통해 텍스트 분류는 단순히 모델을 학습해 점수를 출력하는 작업이 아니라, 데이터 표현·정답 형식·평가 기준을 일관되게 설계하는 과정이라는 것을 배웠다. 다음에는 같은 데이터에서 TF-IDF + Naive Bayes, LSTM, TextCNN을 가능한 비슷한 조건으로 비교해 보고 싶다. 특히 다음 항목을 직접 기록해 보겠다.

- train/validation/test 분할을 고정한 뒤 모델별 accuracy와 macro F1 비교하기
- 학습 데이터로만 vectorizer와 vocabulary를 만들었는지 점검하기
- LSTM과 TextCNN의 오분류 문장을 10개씩 읽고 오류 원인 분류하기
- 멀티레이블 문제에서 임계값을 바꿀 때 precision·recall·F1이 어떻게 달라지는지 표로 정리하기
- 모델 성능뿐 아니라 학습 시간, 파라미터 수, 해석 가능성까지 함께 비교하기

앞으로는 “어떤 모델이 가장 좋은가?”보다 “현재 데이터와 과업에서 어떤 기준선부터 만들고, 어떤 근거로 다음 모델을 선택할 것인가?”를 먼저 생각하는 습관을 들이고 싶다.
